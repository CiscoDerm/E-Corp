name: Check MkDocs Documentation

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

jobs:
  verify-docs:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🏗️ Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs mkdocs-material

      - name: 🔎 Run MkDocs Build Check
        id: build
        run: |
          echo "🛠️ Checking MkDocs build..."
          mkdocs build | tee mkdocs_log.txt || echo "BUILD_FAILED=1" >> $GITHUB_ENV

      - name: 📖 Show MkDocs Logs
        run: |
          if [ -s mkdocs_log.txt ]; then
            cat mkdocs_log.txt
          else
            echo "No logs generated by MkDocs."
          fi

      - name: ❗ Detect Warnings
        id: detect_warnings
        run: |

          if [ -s mkdocs_log.txt ] && grep -i "WARNING" mkdocs_log.txt; then
            echo "❌ Warnings detected in MkDocs logs!"

            echo "HAS_WARNINGS=1" >> $GITHUB_ENV
          else
            echo "No warnings detected."
          fi

      - name: ⚠️ Check Log Length
        id: log_length
        run: |
          LOG_LINES=$(wc -l < mkdocs_log.txt)
          if [ "$LOG_LINES" -gt 3 ]; then
            echo "❌ Too many log lines detected! ($LOG_LINES lines)"
            echo "LOG_LINES_EXCEEDED=1" >> $GITHUB_ENV
          fi

      - name: ❌ Fail on Build Error
        if: env.BUILD_FAILED == '1'
        run: |
          echo "❌ MkDocs build failed! Check the logs above."
          exit 1

      - name: ❌ Fail on Warnings
        if: env.HAS_WARNINGS == '1'
        run: |
          echo "❌ Warnings detected in MkDocs logs! Failing the pipeline."
          exit 1

      - name: ❌ Fail on Excessive Log Lines
        if: env.LOG_LINES_EXCEEDED == '1'
        run: |
          echo "❌ Too many log lines detected! Failing the pipeline."
          exit 1



      - name: ✅ Success Message

        if: env.BUILD_FAILED != '1' && env.LOG_LINES_EXCEEDED != '1' && env.HAS_WARNINGS != '1'

        run: echo "✅ MkDocs build passed!"

      - name: 🐛 Debug Information
        run: |
          echo "Current directory: $(pwd)"
          echo "Files in directory:"
          ls -la
          echo "Content of mkdocs_log.txt:"
          cat mkdocs_log.txt || echo "No logs found."